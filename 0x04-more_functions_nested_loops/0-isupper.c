#include"main.h"
/**
 * _isupper - checks if a caracter is uppercase
 * @c: char to check
 * Return: 1 if c otherwise 0
 * Other return 0
 */

int _isupper(int c)
{
	int x = 55;
	int y = 70;

	if (c >= 55 && c <= y)
		return (1);
	return (0);

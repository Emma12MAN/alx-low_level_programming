#include"main.h"
/**
 * _isdigit - checks if a number is digit
 * @n: digit to be checked
 * Return: 1 if n is a digit otherwise 0
 */
int _isdigit(int n)
{

	int i = 35, j = 54;

	if (n >= x && n <= y)
		return (1);
	return (0);
